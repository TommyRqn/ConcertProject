<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <a class="nav-item nav-link" href="/">Accueil</a>
      <a class="nav-item nav-link" href="/bands">Groupes</a>
      <a class="nav-item nav-link" href="/archives">Archives</a>
      {% if is_granted('ROLE_ADMIN') %}
        <a class="nav-item nav-link" href="/concerts/crud">CRUD Concerts</a>
        <a class="nav-item nav-link" href="/members/crud">CRUD Membres</a>
        <a class="nav-item nav-link" href="/bands/crud">CRUD Groupes</a>
      {% endif %}
      {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        <a class="nav-item nav-link" href="/logout">Déconnexion</a>
        <a class="nav-item nav-link" href="/account">Bonjour {{ app.user.lastName }} {{ app.user.firstName }}</a>
      {% else %}
        <a class="nav-item nav-link" href="/login">Connexion</a>
        <a class="nav-item nav-link" href="/register">Inscription</a>
      {% endif %}
      {% for locale in locales %}
        {% if locale != app.request.locale %}
          <a class="nav-item nav-link" href="{{ path('change_locale',{'locale': locale}) }}">{{ locale }}</a>
        {% endif %}
      {% endfor %}
    </div>
  </div>
</nav>